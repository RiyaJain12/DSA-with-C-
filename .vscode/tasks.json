{
    "tasks": [
        {
            "type": "shell",
            "label": "Run C++ Program:g++.exe",
            "command": "C:\\msys64\\mingw64\\bin\\g++.exe -g ${file} -o ${fileDirname}\\${fileBasenameNoExtension}.exe &&${fileDirname}\\${fileBasenameNoExtension}.exe",
            
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}